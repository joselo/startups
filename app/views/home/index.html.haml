= content_for :sidebar do
  = render 'sidebar'

= page_header

- unless @startups.empty?
  - @startups.each do |startup|
    .startup= render 'shared/startup', :startup => startup

  = paginate @startups

- else
  = t('.there_is_not_startups')
