%ul
  - if user_signed_in?
    %li
      #{t('nav_user.logged_in_as')} #{ link_to current_user.name, me_profile_path}
    %li.last
      = link_to t('nav_user.logout'), signout_path
  - else
    %li.last
      = link_to t('nav_user.login'), signin_path